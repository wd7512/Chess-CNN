"""Convert 64 piece labels to/from FEN strings.

Interface:
    labels_to_fen(labels: list[str]) -> str
    fen_to_labels(fen: str) -> list[str]
"""

import chess


def labels_to_fen(labels):
    """Convert a list of 64 piece labels into a FEN piece-placement string."""
    pass


def fen_to_labels(fen):
    """Convert a FEN piece-placement string into a list of 64 labels."""
    pass
