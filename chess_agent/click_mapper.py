"""Map UCI moves to pixel coordinates on the board.

Handles both white and black perspectives.
Interface:
    uci_to_pixels(uci: str, board_rect: dict, is_white: bool) -> tuple[float, float, float, float]
"""


def uci_to_pixels(uci, board_rect, is_white):
    """Convert a UCI move string to source/dest pixel coordinates."""
    pass
