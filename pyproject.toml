[project]
name = "chess-cnn"
version = "0.1.0"
description = "Chess CNN agent"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "chess>=1.11.2",
    "pytest>=9.1.1",
]

[tool.setuptools.packages.find]
include = ["chess_agent*"]
